# 生成物（含真实出口 IP 清单），禁止提交
agent/.env
scheduler/haproxy-servers.cfg

# SOCKS5 访问认证凭证（含密码），禁止提交
agent/socks-credentials.env

# 代理机唯一清单（含明文密码），禁止提交
deploy/hosts.txt

# 出口 IP 排除名单（含真实 IP），禁止提交
deploy/exclude-ips.txt

# 本地工作区数据（会话日志等），不上远端
.workbuddy/
