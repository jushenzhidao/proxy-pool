# 新代理机纳管清单（供 deploy/provision.sh 使用）
#
# 用法：
#   cp deploy/new-hosts.txt.example deploy/new-hosts.txt
#   vi deploy/new-hosts.txt && chmod 600 deploy/new-hosts.txt
#   ./deploy/provision.sh            # 纳管（装公钥 + Docker + 防火墙 + 入清单）
#   ./deploy/provision.sh --deploy   # 纳管后立刻全量部署
#
# 每行一台机器，字段以空格分隔： <host> <user> <password> [port]
#   · 支持写法 user@host password [port]（等价）
#   · port 省略时默认 22
#   · # 开头为注释，空行忽略
#
# ⚠️ 本文件含明文密码：
#   · 复制成 new-hosts.txt 后请 chmod 600，且不要提交进 git（.gitignore 已排除）
#   · 纳管完成后建议删除：shred -u deploy/new-hosts.txt

# 示例：
# 198.51.100.10 root MyStrongPass
# 198.51.100.11 root AnotherPass 2222
# root@198.51.100.12 ThirdPass
