# 复制为 exclude-ips.txt 后生效（一行一个 IP，# 开头为注释）
#
# 用途：机器上 `ip -4 addr` 能扫到、但不希望作为出口的 IP，会在 deploy.sh
#       汇总时被跳过，不写进 scheduler/haproxy-servers.cfg。
#
# 典型场景：
#   - 云商辅助 IP 已绑到网卡，但回程路由没配通（能 bind 但出不了网）
#   - 管理口 / 内网互通地址 / 临时网卡
#   - 某台机器只想用主 IP，不用辅助 IP
#
# 等价的环境变量写法（不用文件也行）：
#   EXCLUDE_IPS="1.2.3.4,5.6.7.8" ./deploy.sh
#
# 注意：本文件示例不含真实 IP；exclude-ips.txt 若含真实 IP 请保持 600 权限，
#       它已在 .gitignore 中排除。

# 198.51.100.99
# 203.0.113.77
